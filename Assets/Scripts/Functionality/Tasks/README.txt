Summary:
Implements logic that needs to access the database. Services are not allowed to access the engine or graphics directly, but might do so indirectly with callbacks.

Contains:
usea.core.service

Allowed dependencies:
         Data : usea.data
      Utility : usea.util
 Sub services : usea.core.subService